CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
# RGB status display calls central-only functions (layer state, BLE profile, endpoints).
# Disable underglow entirely on the peripheral half.
CONFIG_ZMK_RGB_UNDERGLOW=n
